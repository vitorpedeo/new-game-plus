import React from 'react';

import logo from '../../assets/logo.svg';

import './styles.scss';

const LogoHeader = () => {
  return (
    <header>
      <img src={logo} alt='' />
    </header>
  );
};

export default LogoHeader;
